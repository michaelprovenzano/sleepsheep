extends base

block content
  .content
    .container
      form#reset-password-form.form.login-form(action='' data-token=token)
        input(type='password' name='new-password' id='new-password' placeholder='New Password' required='')
        input(type='password' name='new-password-confirm' id='new-password-confirm' placeholder='Confirm New Password' required='')
        button.btn.btn-primary(type='submit') Set Password